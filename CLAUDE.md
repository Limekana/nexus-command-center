# NCC
Ecosystem: D:\emilh\Projects\CLAUDE.md
Extras: `npm run icons` · `cd android && ./gradlew-quiet assembleDebug`

## Logging — MANDATORY (owner rule, 2026-09-18)
The ecosystem file above is on the owner's machine, and cloud agents cannot read it. This is the part they must not miss:
- **Open work lives in GitHub Issues** (Project: **Limecore Suite**). Non-sensitive items for this app go here. Ops, security and business items go in private `Limekana/limecore`. Run `gh issue list` before starting.
- Open an issue for every new bug, follow-up or deferral as soon as you find it. Never leave one only in chat or a plan.
- PRs say `Closes #N` (cross-repo: `Closes Limekana/limecore#N`). Touched issues get a comment or are closed.
- If a version, status or decision changed, update `Limekana/limecore` → `Nexus_Version_Status.md` (pull it first, then push). If you cannot push, put the outcome in an issue comment instead.
- A task is not done until this is done.
